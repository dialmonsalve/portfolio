---
import Layout from "../../layouts/Layout.astro";

import { getI18N } from "../../i18n";

const currentLocale = Astro.currentLocale;

const i18n = getI18N({ currentLocale });
---

<Layout>
  <h1 class="text-primary">{i18n.HOME.TITLE}</h1>
</Layout>
